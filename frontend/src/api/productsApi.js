export const productsApi = {
  getAll: async () => {},
  getById: async (id) => {},
  search: async (query) => {},
  create: async (product) => {},
  update: async (id, product) => {},
  delete: async (id) => {},
}
